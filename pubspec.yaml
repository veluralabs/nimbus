name: nimbus
description: "Nimbus — open-source, self-hosted photo & media backup for Android. Back up to your own Google Cloud Storage, free local space with verified offload, on-device face grouping, and AI categories/editing."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Google Cloud Storage REST API + service-account auth
  googleapis: ^14.0.0
  googleapis_auth: ^2.0.0

  # Runtime permission requests (MANAGE_EXTERNAL_STORAGE) + open settings
  permission_handler: ^12.0.0

  # MD5 hashing for upload verification
  crypto: ^3.0.6

  # Path helpers + readable file sizes
  path: ^1.9.0

  # MediaStore access: albums, assets, thumbnails (the right way vs raw scan)
  photo_manager: ^3.6.1

  # Local index DB for dedup + labels + face groups
  sqflite: ^2.4.1
  path_provider: ^2.1.5

  # Background upload scheduling
  workmanager: ^0.9.0+3

  # On-device face embeddings
  tflite_flutter: ^0.11.0

  # Decode + crop face regions before embedding
  image: ^4.3.0

  # Auth: Firebase Identity Toolkit REST + session persistence
  http: ^1.6.0
  shared_preferences: ^2.3.3

  # Free on-device face detection (boxes) — replaces paid Vision face feature
  google_mlkit_face_detection: ^0.13.1

  # In-app video playback + full controls UI (scrub, fullscreen, speed, mute)
  video_player: ^2.9.2
  chewie: ^1.8.5

  # Share sheet from the photo viewer
  share_plus: ^10.1.4

  # Shimmer loading placeholders
  shimmer: ^3.0.0

  # Open documents/audio with the system's default app
  open_filex: ^4.5.0

  # Native HEIC/odd-JPEG -> JPEG thumbnailing fallback (Android codecs)
  flutter_image_compress: ^2.3.0

  # Reverse-geocode photo GPS to place names (free, on-device platform geocoder)
  geocoding: ^3.0.0

  # Read EXIF GPS directly from the original file (robust location fallback)
  native_exif: ^0.6.2

  # Foreground service: keep uploading when the app is closed + status-bar notif
  flutter_foreground_task: ^8.17.0

  # Network type (WiFi vs mobile) + battery level/charging awareness
  connectivity_plus: ^6.1.0
  battery_plus: ^6.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Generates Android launcher icons from assets/icon/
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/nimbus_icon.png"
  min_sdk_android: 26
  adaptive_icon_background: "#6A5CF2"
  adaptive_icon_foreground: "assets/icon/nimbus_icon_fg.png"

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The whole assets/ dir is bundled so the project builds even before you add a
  # key. Drop your GCS service-account key at assets/service_account.json (copy
  # the .example) and it gets picked up automatically. The MobileFaceNet model
  # (assets/models/mobilefacenet.tflite) enables face grouping — see README.
  assets:
    - assets/
    - assets/models/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
