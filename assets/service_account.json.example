{
  "_README": "REPLACE THIS FILE with your real GCS service-account key JSON.",
  "_HOW": "GCP Console -> IAM & Admin -> Service Accounts -> create one scoped to ONLY your backup bucket (role: Storage Object Admin on that bucket) -> Keys -> Add Key -> JSON. Paste the downloaded JSON over this whole file.",
  "_SECURITY": "Anyone who extracts the APK can read this key. Keep the bucket scope minimal and rotate the key if the APK leaks.",
  "type": "service_account",
  "project_id": "REPLACE_ME",
  "private_key_id": "REPLACE_ME",
  "private_key": "-----BEGIN PRIVATE KEY-----\nREPLACE_ME\n-----END PRIVATE KEY-----\n",
  "client_email": "REPLACE_ME@REPLACE_ME.iam.gserviceaccount.com",
  "client_id": "REPLACE_ME",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "REPLACE_ME"
}
